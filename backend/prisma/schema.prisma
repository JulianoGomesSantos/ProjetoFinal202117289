// Este arquivo é utilizado pelo prisma para gerar o schema do banco de dados
// Para gerar o schema, execute o comando: npx prisma generate
// Para criar o banco de dados, execute o comando: npx prisma migrate dev --name init
// Para criar uma nova migração, execute o comando: npx prisma migrate dev --name <nome da migração>
// Para aplicar as migrações, execute o comando: npx prisma migrate dev
// Para reverter as migrações, execute o comando: npx prisma migrate reset
// Para reverter uma migração, execute o comando: npx prisma migrate reset --name <nome da migração>
// Para criar um novo model, execute o comando: npx prisma migrate dev --name <nome da migração>

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
  url      = "mysql://root:my-secret-pw@localhost:3306/mydatabase"
}

model tasks {
  id          Int            @id @default(autoincrement())
  task_name   String         @db.VarChar(30)
  description String?        @db.VarChar(60)
  priority    tasks_priority
  userId      Int
  created_at  DateTime?      @db.DateTime(0)
  updated_at  DateTime?      @db.DateTime(0)
}

enum tasks_priority {
  low
  high
  medium
}
